# ai-skills

> Artifact-producing skills for AI agents. Generates files that LLMs cannot produce from their token stream: QR codes (SVG), PDFs, spreadsheets (CSV/TSV), iCalendar (.ics), and charts (SVG). Zero external dependencies.

## Use instantly (MCP)

Add to any MCP-compatible agent (Claude Desktop, Cursor, Cline, etc.):

```json
{ "command": "npx", "args": ["ai-skills", "serve"] }
```

This gives the agent 5 tools: `generate_qr_code`, `build_pdf`, `build_spreadsheet`, `generate_ical`, `generate_chart`.

## Use as a library

```bash
npm install ai-skills
```

```typescript
import { skills } from "ai-skills";

skills.generateQrCode({ data: "https://example.com" })           // → SVG string
skills.buildPdf({ title: "Report", body: ["Hello."] })           // → { base64, size, pages }
skills.buildSpreadsheet({ headers: ["A"], rows: [["1"]] })       // → CSV string
skills.generateIcal({ events: [{ summary: "Meeting", start: "2025-03-15T14:00:00", end: "2025-03-15T15:00:00" }] }) // → .ics string
skills.generateChart({ type: "bar", data: { Q1: 100, Q2: 200 } }) // → SVG string
```

## Skills

- `generateQrCode({ data, size?, ecl?, fg?, bg? })` → SVG
- `buildPdf({ title?, author?, date?, body, footer?, pageSize? })` → `{ base64, size, pages }`
- `buildSpreadsheet({ headers, rows, summary?, format?, formulas?, bom? })` → CSV/TSV string
- `generateIcal({ events, calendar_name?, method? })` → .ics string
- `generateChart({ type, data, title?, width?, height?, colors? })` → SVG

## Full API reference

- [llms-full.txt](https://github.com/edceds/ai-skills/blob/main/llms-full.txt)
